import React from 'react'
import PageInfo from '../pageinfo'

const About = () => {
  return (
    <PageInfo>
    <h1 className='text-center text-2xl'>Peach Note</h1><br />
      Hello This is my first chat app project that relies solely on database connectivity without use of tools such as firebase and Socket. This app has client side authentication during login as authentication mechanism as a sole CIA mechanism.
    </PageInfo>
  )
}

export default About
